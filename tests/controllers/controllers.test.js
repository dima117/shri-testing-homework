const { expect } = require('chai');
const sinon = require('sinon');

const { buildObjectUrl } = require('../../controllers/filesController');

describe('contentController', () => {
  it('test', () => {});
});

describe('filesController', () => {
  it('test', () => {});
});

describe('indexController', () => {
  it('test', () => {});
});
