let {expect} = require('chai'),
    Git = require('../../utils/git'),
    { gitHistory, gitFileTree, gitFileContent } = require('../../utils/git');


describe.only('git.js', function () {

    context('gitHistory()', function () {
        describe('', function () {
            it('', function () {
                // Подготовка
                let page = 1,
                    offset = 10;
                // Действие
                // Проверка
            });
        });
        describe('executeGit()', function () {
            it('', function () {
                // Подготовка
                Git.executeGit();
                // Действие
                // Проверка

            });
        });
    });

});
