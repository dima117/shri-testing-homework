<div class="breadcrumbs">
    {{#each breadcrumbs}}
        {{#if href}}
            <a class="breadcrumbs__link" href="{{href}}">{{text}}</a> /
        {{else}}
            {{text}}
        {{/if}}
    {{/each}}
</div>

<div class="content content--index">
    {{#each list}}
        <div class="commit">
            <div class="commit__info">
                <span class="commit__author">{{author}}</span>
                <span class="commit__date">{{timestamp}}</span>
            </div>
            <div class="commit__msg">
                {{msg}}
            </div>
            <div class="commit__link">
                <a class="commit__href" href="{{href}}">{{hash}}</a>
            </div>
        </div>
    {{/each}}
</div>
